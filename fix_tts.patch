--- app/src/main/java/com/finverse/ui/FinverseViewModel.kt
+++ app/src/main/java/com/finverse/ui/FinverseViewModel.kt
@@ -10,6 +10,8 @@
 import android.app.Application
 import android.content.Context
 import android.media.MediaPlayer
+import android.speech.tts.TextToSpeech
+import android.speech.tts.UtteranceProgressListener
 import android.util.Base64
 import androidx.lifecycle.AndroidViewModel
 import androidx.lifecycle.ViewModel
@@ -28,6 +30,7 @@
 import kotlinx.coroutines.launch
 import java.io.File
 import java.text.NumberFormat
+import java.util.UUID
 import java.util.Locale
 
 sealed class UIState {
@@ -64,12 +67,26 @@
     val aiAudioBase64 = _aiAudioBase64.asStateFlow()
 
     private val _isPlayingAudio = MutableStateFlow(false)
     val isPlayingAudio = _isPlayingAudio.asStateFlow()
 
-    private var mediaPlayer: MediaPlayer? = null
+    private var tts: TextToSpeech? = null
+
+    init {
+        tts = TextToSpeech(application) { status ->
+            if (status == TextToSpeech.SUCCESS) {
+                tts?.language = Locale("en", "IN")
+                tts?.setOnUtteranceProgressListener(object : UtteranceProgressListener() {
+                    override fun onStart(utteranceId: String?) { _isPlayingAudio.value = true }
+                    override fun onDone(utteranceId: String?) { _isPlayingAudio.value = false }
+                    override fun onError(utteranceId: String?) { _isPlayingAudio.value = false }
+                })
+            }
+        }
+    }
 
     fun formatCurrency(amount: Double): String {
@@ -181,9 +198,7 @@
                     _aiAudioBase64.value = responseBody.audioBase64
                     _uiState.value = UIState.Success("Advice generated")
                     
-                    // Auto play audio voice if present
-                    if (!responseBody.audioBase64.isNullOrBlank()) {
-                        playAiAudio()
-                    }
+                    // Auto play audio voice for all responses using TTS
+                    playAiAudio()
                 } else {
                     // Smart built-in financial intelligence fallback
@@ -191,10 +206,12 @@
                     _uiState.value = UIState.Success("Advice generated")
+                    playAiAudio()
                 }
             } catch (e: Exception) {
                 // Graceful local financial intelligence fallback when offline
-                _aiResponse.value = "[Network Error]\n\nHi ${currentProfile.userName}, it looks like you are offline. Tech Dev & Finverse AI requires an internet connection to process complex queries.\n\n" + generateLocalFinancialAdvice(question, currentProfile, monthlySavings, health.overall)
+                _aiResponse.value = "Hi ${currentProfile.userName}, it looks like you are offline. Tech Dev & Finverse AI requires an internet connection to process complex queries.\n\n" + generateLocalFinancialAdvice(question, currentProfile, monthlySavings, health.overall)
                 _uiState.value = UIState.Error("Network Error: Tech Dev & Finverse AI is offline.")
+                playAiAudio()
             }
         }
     }
@@ -200,38 +217,16 @@
 
     fun playAiAudio() {
-        val b64 = _aiAudioBase64.value ?: return
+        val responseText = _aiResponse.value ?: return
         try {
             stopAiAudio()
-            val audioBytes = Base64.decode(b64, Base64.DEFAULT)
-            val tempFile = File.createTempFile("bol_ai_response_", ".wav", getApplication<Application>().cacheDir)
-            tempFile.writeBytes(audioBytes)
             
-            mediaPlayer = MediaPlayer().apply {
-                setDataSource(tempFile.absolutePath)
-                prepare()
-                setOnCompletionListener {
-                    _isPlayingAudio.value = false
-                }
-                start()
-            }
-            _isPlayingAudio.value = true
+            // Clean markdown syntax for TTS speaking
+            val cleanText = responseText.replace(Regex("[*#\\[\\]]"), "")
+            val utteranceId = UUID.randomUUID().toString()
+            
+            tts?.speak(cleanText, TextToSpeech.QUEUE_FLUSH, null, utteranceId)
         } catch (e: Exception) {
             e.printStackTrace()
             _isPlayingAudio.value = false
         }
     }
 
     fun stopAiAudio() {
         try {
-            mediaPlayer?.let {
-                if (it.isPlaying) {
-                    it.stop()
-                }
-                it.release()
-            }
+            tts?.stop()
         } catch (e: Exception) {
             e.printStackTrace()
         } finally {
-            mediaPlayer = null
             _isPlayingAudio.value = false
         }
     }
 
     override fun onCleared() {
         super.onCleared()
         stopAiAudio()
+        tts?.shutdown()
     }
